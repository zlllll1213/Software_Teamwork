# Fix PR 300 review findings

## Goal

Address the two actionable findings generated by Codex for PR #300 without expanding the PR into the broader cross-service RAG acceptance task.

## Requirements

- Make the `missing_profile` smoke probe use a request-scoped profile ID that cannot be predictably derived from the configured valid profile.
- Preserve the existing expectation that the probe is normalized to `dependency_error` and does not expose secrets.
- Replace the seed-only context records in the archived issue #288 Trellis task with the specifications and research actually used.
- Replace the issue #288 journal placeholders with concrete implementation and verification details.
- Keep the unrelated `.pr237.diff` file untouched.

## Acceptance Criteria

- [x] `missing_profile` uses a request-scoped missing profile ID.
- [x] QA formatting and tests pass with the external smoke gate disabled.
- [x] The archived task's `implement.jsonl` and `check.jsonl` contain no Trellis template seed rows.
- [x] The issue #288 journal entry contains no journal template placeholders.
- [x] No central Codex workflow permission change is bundled into PR #300.

## Definition of Done

- Relevant QA tests and build checks pass.
- Trellis quality review is complete.
- Changes are committed, the follow-up task is archived, the session is recorded, and the branch is pushed.

## Technical Approach

Use the already unique `requestID` as the prefix of a deliberately nonexistent profile ID. Record the backend specifications and original smoke-design research in the archived task context, and replace journal placeholders with the commands and outcomes already evidenced by the original implementation session and CI.

## Decision (ADR-lite)

**Context**: Deriving `-missing` from a real profile name can collide with an existing profile, while changing the central PR-review workflow would be unrelated to issue #288.

**Decision**: Use `requestID + "-missing-profile"` for the negative probe and keep the workflow permission regression out of this PR.

**Consequences**: Each invocation gets an effectively unique missing-profile candidate; the failing Codex publication check still requires a separate `develop` workflow fix.

## Out of Scope

- The broader seed-to-citations RAG end-to-end acceptance task included in the user-provided metadata.
- Changes to `.github/workflows/codex-pr-review.yml`.
- Posting or resolving GitHub review threads; the findings only exist in the failed workflow log.

## Technical Notes

- PR: https://github.com/Sakayori-Iroha-168/Software_Teamwork/pull/300
- Original task: `.trellis/tasks/archive/2026-06/06-30-issue-288-qa-ai-gateway-smoke/`
- Smoke test: `services/qa/internal/platform/modelclient/ai_gateway_smoke_test.go`
